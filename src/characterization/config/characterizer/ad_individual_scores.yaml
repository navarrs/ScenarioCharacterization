# @package _global_
defaults:
  - ad_default.yaml
  - ad_individual_features.yaml

processor:
  _target_: characterization.processors.scores_processor.ScoresProcessor
  config:
    output_path: ${paths.output_paths.scores_cache_path}

characterizer:
  _target_: characterization.domains.ad.scorer.individual_scorer.IndividualScorer
